import { showAmenity } from '../../../helpers/show-amenity';
import { module, test } from 'qunit';

module('Unit | Helper | show amenity');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = showAmenity(42);
  assert.ok(result);
});
