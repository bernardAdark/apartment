import { pluralizeText } from '../../../helpers/pluralize-text';
import { module, test } from 'qunit';

module('Unit | Helper | pluralize text');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = pluralizeText(42);
  assert.ok(result);
});
