import { valueOrDefault } from '../../../helpers/value-or-default';
import { module, test } from 'qunit';

module('Unit | Helper | value or default');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = valueOrDefault(42);
  assert.ok(result);
});
